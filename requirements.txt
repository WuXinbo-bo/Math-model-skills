# Meta-model-skills-max 依赖
# 基于 scripts/ 目录实际 import 扫描结果（2026-06-16）

# 文档处理（6个文件使用）
python-docx>=0.8.11
lxml>=4.9.0

# LaTeX 公式渲染（2个文件硬依赖：build_docx.py, formula_renderer.py）
latex2mathml>=3.0.0
mathml2omml>=0.2.0

# 数据处理
pandas>=1.5.0
numpy>=1.23.0

# 可视化
matplotlib>=3.6.0

# YAML 配置（3个文件使用）
pyyaml>=6.0

# 科学计算（可选，data_preprocessor.py 有条件使用）
scipy>=1.9.0
